import { DefaultThemeDirective } from './default-theme.directive';

describe('DefaultThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultThemeDirective();
    expect(directive).toBeTruthy();
  });
});
